server:
  port: 8761
eureka:
  instance:
    hostname: pds-ppi.herokuapp.com
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}/eureka